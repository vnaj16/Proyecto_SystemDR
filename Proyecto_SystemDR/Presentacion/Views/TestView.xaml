<Window x:Class="Presentacion.Views.TestView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Presentacion.Views"
        mc:Ignorable="d"
        Title="TestView" Height="450" Width="800"
        xmlns:vm="clr-namespace:Presentacion.ViewModels">
    <Window.Resources>
        <vm:ClienteViewModel x:Key="ClienteVM"/>
    </Window.Resources>
    <Grid DataContext="{StaticResource ClienteVM}" Name="ClienteView">
        <DataGrid Name="DG_Cliente"  ItemsSource="{Binding Lista_Clientes}" SelectedItem="{Binding Current_Cliente}" HorizontalAlignment="Left" 
                  Height="267" Margin="10,34,0,0" VerticalAlignment="Top" Width="772">

        </DataGrid>
        <Label Content="{Binding Current_Cliente.Persona.Nombre}" HorizontalAlignment="Left" Margin="79,335,0,0" VerticalAlignment="Top"/>
        <ListBox HorizontalAlignment="Left" Height="100" ItemsSource="{Binding Lista_Clientes}" SelectedItem="{Binding Current_Cliente}" Margin="460,309,0,0" VerticalAlignment="Top" Width="180"/>


    </Grid>
</Window>
